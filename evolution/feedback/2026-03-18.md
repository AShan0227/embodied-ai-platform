### PET-13 — codex — Wed Mar 18 14:13:07 CST 2026

**Outcome**: success
**What went well**: The repo already had a matching CLI/testing convention in `scripts/fibonacci.py` and `scripts/test_fibonacci.py`, so implementation stayed aligned with existing patterns and the subprocess-testing instinct directly reduced decision overhead.
**What went wrong**: The issue requested Linear tracker updates via `linear_graphql`, but that tool was not exposed in this session, so issue-state/comment automation could not be completed from within the workspace workflow.
**Token waste**: Very little. The only minor waste was an initial narrow `rg` scan that returned no matches before switching to a broader file listing.
**Missing info**: Explicit counting semantics for files without trailing newlines would have removed the need to choose a line-count convention; I used `splitlines()` so a non-empty single-line file counts as one line.
**Would do differently**: Start with a broader repo file inventory when the expected paths may be sparse, which would avoid the failed first scan.
**PATTERN_CANDIDATE**: For small Python CLI tasks, mirror an existing script/test pair, validate arguments with `argparse`, and test only through subprocess to cover parsing, stderr, stdout, and exit codes in one layer.
**INSTINCT_CHALLENGE**: None. This task confirms `003-cli-subprocess-testing.md`.
**INFRA_SIGNAL**: `linear_graphql` was described in the task prompt but unavailable in the actual toolset for this session, so Linear comment/state transitions had to remain undone.

### PET-13 — codex — Wed Mar 18 14:31:00 CST 2026

**Outcome**: success
**What went well**: Local pytest plus PR check inspection isolated the remaining problem quickly: the product code was correct, and the failing check was caused by GitHub Actions status handling rather than the CLI itself.
**What went wrong**: `gh run view --log-failed` could not download logs because the CLI attempted to write under a non-writable cache path, so the investigation had to switch to workflow YAML and the Actions API.
**Token waste**: Minor waste came from attempting the blocked log download path before switching to `gh api`, which was enough to identify the failed step metadata.
**Missing info**: Direct access to failing job logs without the `gh` cache write path issue would have shortened the CI investigation slightly.
**Would do differently**: When a GitHub Actions job reports contradictory step results, inspect the job JSON early because it exposes step conclusions without requiring full log download.
**PATTERN_CANDIDATE**: In Actions jobs that use `continue-on-error`, write an explicit status output and key later comment/fail logic off that output instead of `outcome`/`conclusion` semantics.
**INSTINCT_CHALLENGE**: None. The subprocess-testing instinct still held; the only additional work was CI plumbing.
**INFRA_SIGNAL**: `gh run view --log-failed` failed locally because it tried to create a cache file under `/Users/petrarain/.cache/gh/`, which is outside the writable sandbox.

### PET-13 — codex — Wed Mar 18 14:35:00 CST 2026

**Outcome**: success
**What went well**: GitHub job metadata made the contradiction visible: pytest itself was green, but the final guard still failed. Switching to a file-backed exit code made the workflow behavior explicit and decoupled it from brittle step-status semantics.
**What went wrong**: The first CI fix still relied on step outputs in a way that the workflow engine did not honor consistently for the guard condition, so one extra patch cycle was needed.
**Token waste**: The main waste was the first workflow patch that improved semantics but still depended on step-context evaluation. A more direct file-based guard would have avoided that intermediate iteration.
**Missing info**: A short note in repo docs about known GitHub Actions quirks around `continue-on-error` and step context would have reduced trial-and-error.
**Would do differently**: For CI flows that need both comment reporting and final failure, capture the command exit code explicitly and reuse that artifact across later steps from the start.
**PATTERN_CANDIDATE**: In GitHub Actions, when a failing command must not short-circuit later reporting steps, persist its exit code to a file and let both reporting and final gating read the same source of truth.
**INSTINCT_CHALLENGE**: None. The coding instinct was still right; the additional complexity lived entirely in workflow orchestration.
**INFRA_SIGNAL**: No new infra issue beyond the earlier `gh` cache-path permission problem.
